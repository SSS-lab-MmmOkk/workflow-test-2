name: Debug Claude Trigger

on:
  issue_comment:
    types: [created]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Show comment
        run: echo "Comment: '${{ github.event.comment.body }}'"
