name: Trigger Test

on:
  issue_comment:
    types: [created]

jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - name: Show the comment
        run: echo "Comment was: ${{ github.event.comment.body }}"
